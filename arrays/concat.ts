// syntax : yourArray.concat(tobe_add_array)
// creates new array by adding given elements
// original array remain unaltered

const ar : number[] = [1,2,3,4,5];

console.log(ar.concat([8,9,8])); // [1, 2, 3, 4, 5, 8, 9, 8] // you can store in new variable

console.log(ar); // [1, 2, 3, 4, 5]



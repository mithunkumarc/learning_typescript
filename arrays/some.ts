// some(function) : 
// Determines whether the specified callback function 
// returns true for any element of an array.
const ar: number[] = [11,22,33,44,55,33];
// one of the element is 44
console.log(ar.some(e => e == 44))
